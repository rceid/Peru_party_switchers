README file for party switching application
Date: March 16, 2020

To run the party switching application, please follow the steps below:

	1) Please install Google Chrome, if you have not already.

	2) In the "/project" directory, run the install shell script with the
	   command '$ sh install.sh'.

	3) Activate the virtual environment with the command '$ source
	   env/bin/activate'.

	4) Start up the application's interface by running the command
	   '$ ipython party_switching.py' or '$ python3 party_switching.py'.

Summary of directorys and files in the "/project" directory:

code (directory): Stores all the code files that are used in the application

data (directory): Stores all the data files that are used in the application

output (directory): Empty folder where web-scraped data and generated
					visualizations are stored

install.sh (file): Shell script for installation

README.txt (file): This file

party_switching.py (file): Main file for running application

requirements.txt (file): Required packages for running application (installed
					     via "install.sh")

The Party Switchers.pptx (file): Powerpoint documenting project details

REQUIRED LIBRARIES:

cycler==0.10.0
decorator==4.4.2
kiwisolver==1.1.0
matplotlib==3.2.0
networkx==2.4
numpy==1.18.1
pandas==1.0.2
plotly==4.5.4
pydot==1.4.1
pyparsing==2.4.6
python-dateutil==2.8.1
pytz==2019.3
retrying==1.3.3
scipy==1.4.1
seaborn==0.10.0
selenium==3.141.0
six==1.14.0
urllib3==1.25.8
xlrd==1.2.0
